# NextOps project status brief

[فارسی](../fa/PROJECT_STATUS_BRIEF.md) · [Documentation index](INDEX.md) · [Project state](../PROJECT_STATE.md) · [Next task](../NEXT_TASK.md)

Updated: 2026-09-22

## Executive position

NextOps has reached a controlled user-testing checkpoint. The first complete read-only path is live
across the four Ubuntu 24.04 servers: a user signs in through the private TLS application panel,
requests an investigation in English or Persian, receives current evidence from Zabbix through the
restricted connector, and obtains an explanation generated by the local CPU-only model.

The deployed slice is suitable for supervised user testing, not production acceptance. It covers
four approved hosts and a bounded summary operation. Durable investigation/audit, scoped failure
handling, server/API WAN isolation and serial VM restart have passed. An independently isolated
fresh browser, sustained load, artifact rollback, independent backup and isolated restore remain
open.

## Status at a glance

| Workstream | Current status | Verified result | Remaining acceptance |
|---|---|---|---|
| Architecture and governance | Accepted baseline | Four-server, CPU-only, local-inference and read-only-first boundaries remain enforced | Complete operation-specific audit and the remaining recovery gates |
| Application and database | Live for controlled testing | Immutable application release, PostgreSQL 16, local identity, private TLS, bilingual panel, durable investigation state and append-only audit linkage are active | Prove independent backup/restore, sustained load and production observability |
| Local CPU inference | Live and integrated | Pinned llama.cpp runtime and Qwen model are active on loopback; authenticated bilingual generation, cold process restart and server/API WAN isolation passed | Prove runtime/model rollback, sustained load, cancellation and isolated recovery |
| Zabbix | Live with restricted scope | Zabbix 7.0.30, separate PostgreSQL, frontend and Agent 2 are active; the reader can access four approved hosts through only three read methods | Complete retention, independent backup, isolated restore and remaining resource-failure cases |
| Read-only connector | Live and least-privilege | Rootless loopback service uses protected credentials, strict TLS and an allowlisted summary operation; stale, partial, unavailable, revoked-token and recovery cases passed | Complete low-space/resource-limit qualification and recovery documentation |
| End-to-end user path | Passed for the controlled server/API path | Login, bilingual modes, grounded evidence, durable audit, explicit WAN denial and serial clean VM reboots passed | Repeat the offline workflow in an independently isolated fresh browser and complete load/recovery gates |

## Delivered user-testing capability

- A private HTTPS panel with English left-to-right and Persian right-to-left interfaces.
- Protected local sign-in; no infrastructure credential is exposed to the browser or model.
- A fixed, read-only investigation workflow rather than arbitrary JSON-RPC, URL, command or write
  access.
- Current Zabbix evidence with source version, host, collection time, metric observation time,
  stale-data state and active-problem count.
- A local CPU-generated explanation grounded in the returned evidence, with no cloud inference
  fallback.
- Native systemd deployment with immutable release directories, stable links, root-owned protected
  configuration and credentials, loopback backends, private TLS and host firewalls.
- Separate restricted SSH tunnels from the application server to the AI and connector services.

## Acceptance evidence

- Repository checks passed: Ruff formatting and lint, strict mypy across 65 files, documentation,
  deployment, inference and release-status validators, and 103 non-integration tests. Six
  PostgreSQL integration tests have prior isolated-database evidence but could not be rerun in the
  current Windows session because no local PostgreSQL server or working container runtime exists.
- Connector verification returned Zabbix 7.0.30, four approved hosts, eight current metrics, zero
  active problems and zero stale metrics.
- Authenticated end-to-end checks passed for live monitoring and grounded generation in both
  languages. The English and Persian requests each returned the configured 128-token response.
- Browser QA verified the English sign-in presentation and the genuine Persian RTL layout. The full
  authenticated flow was also exercised programmatically against the deployed HTTPS endpoint; an
  independently network-isolated fresh browser remains a separate acceptance gate.
- All four servers reported healthy systemd state, no failed units, no pending package update and no
  required reboot at the deployment checkpoint.

## Security posture

The current slice deliberately limits authority. Zabbix credentials exist only on the connector;
the connector API is loopback-only; its API role permits `host.get`, `item.get` and `problem.get`
for one approved host group; and a non-allowlisted method was verified as denied. The model receives
normalized evidence, not infrastructure credentials, and has no change or approval capability.
Only SSH and the intended private HTTPS endpoint are allowed by the relevant host firewalls.

## Remaining work before production acceptance

1. Prove runtime, model and application rollback from verified local immutable artifacts.
2. Establish PostgreSQL-aware backup plus independent artifact/configuration backup, then pass an
   isolated restore rather than treating backup creation as success.
3. Complete the independently isolated fresh-browser case, sustained load, cancellation,
   low-space/resource-limit and dependency-recovery tests with measured CPU, RAM and latency.
4. Add further production hosts, templates and service-level evidence only through explicit scope
   review; do not broaden connector methods or credentials implicitly.
5. Add operational dashboards, alerting, log retention, certificate/key rotation and a documented
   user-test feedback and defect workflow.

## Presentation conclusion

NextOps now has a functioning, security-bounded product slice rather than only prepared
infrastructure. It is ready for supervised user testing of the bilingual, read-only investigation
experience. Production readiness must wait for independent browser isolation, sustained-load and
recovery evidence, and demonstrated backup, isolated restore and artifact rollback.

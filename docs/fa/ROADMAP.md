<div dir="rtl">

# نقشهٔ راه مرحله‌ای و معیارهای پذیرش

[English](../en/ROADMAP.md) · [شروع کار](START_HERE.md) · [فهرست](INDEX.md) · [برنامهٔ سرورها](SERVER_PLAN.md)

**وضعیت: ترتیب در ۲۱ سپتامبر ۲۰۲۶ پذیرفته شد؛ مرحلهٔ صفر کامل و Incrementهای 1 و 2 از 1A در CI جدا آزموده شده‌اند.** هنوز سرویس مستقر یا جریان کامل مرحلهٔ یک نداریم. مشخصات اصلی، الزام آفلاین، شواهد سخت‌افزاری مالک و تأکید بر اولین پاسخ Zabbix مبنای این طرح‌اند. پذیرش، مجوز ساخت ماشین، تغییر میزبان یا دسترسی عملیاتی نیست.

**اولین تحویل همچنان مرحلهٔ یک است: سؤال تازه، دریافت دادهٔ فقط‌خواندنی و مجاز Zabbix، پاسخ تولیدشده روی CPU محلی، ارجاع منبع و زمان و ثبت ممیزی؛ با اینترنت قطع.** بررسی تکمیلی Linux در مرحلهٔ دو می‌آید. پرامپت بایگانی‌شده تغییر نمی‌کند و زمان‌بندی قدیمیِ اولین پاسخ در مرحلهٔ دو کنار گذاشته شده است.

## ابتدا این ماشین‌ها را بسازید

پس از مجوز جداگانهٔ ساخت، به‌ترتیب **`nextops-app`، سپس `nextops-ai` و سپس `nextops-connectors-ro`** ساخته شوند. منابع پیشنهادی آن‌ها به‌ترتیب **۸/۳۲/۲۰۰**، **۲۴/۱۲۸/۵۰۰** و **۴/۸/۸۰** است؛ ترتیب اعداد vCPU، حافظهٔ GiB و دیسک GiB است. مجموع: **۳ ماشین، ۳۶ vCPU، حافظهٔ ۱۶۸ GiB و دیسک ۷۸۰ GiB**. PostgreSQL ابتدا سرویس مستقل با دسترسی محدود داخل ماشین برنامه است. ماشین مستقل پایگاه و اجرای تغییر فعلاً ساخته نشوند.

[راهنمای شروع](START_HERE.md) ترتیب ساخت ماشین را از پیاده‌سازی نرم‌افزار و بالا آمدن سرویس جدا می‌کند. Zabbix مناسب و مجاز موجود دوباره استفاده شود. در مسیر منتخب سرور جدید، پیش از 1C ماشین مستقل `zabbix-server` با ۴ vCPU، حافظهٔ ۱۶ GiB و دیسک ۲۰۰ GiB آماده شود. جمع اولیه ۴ ماشین، ۴۰ vCPU، حافظهٔ ۱۸۴ GiB و دیسک ۹۸۰ GiB است؛ آزمایشگاه قدیمی هم‌زمان اضافه نشود. اعداد بودجهٔ برنامه‌ریزی‌اند، نه منابع آزاد اندازه‌گیری‌شده یا ادعای ظرفیت پایش.

## مراحل اصلی

| مرحله | خروجی | ماشین‌های NextOps روی یک G10 | شاهد لازم برای عبور |
|---|---|---:|---|
| صفر — پیش‌نیازهای باقی‌مانده و طراحی | حفظ کار مخزن، استفاده از مشخصات و build ارسالی، بررسی منابع آزاد، دیسک، شبکه و بازیابی، سازگاری VM، دسترسی Zabbix، هدف بار، مدل تهدید و فایل‌های آفلاین | صفر ماشین جدید | مالک معماری و آماده‌سازی را تأیید کند؛ مجهول‌ها صریح بمانند. مجموع CPU و RAM و build ارسالی دوباره درخواست نشوند. |
| یک — پایهٔ ایمن و پاسخ‌گویی Zabbix | تکمیل 1A تا 1E: ورود محلی، پایگاه، ممیزی، کار ماندگار، مدل CPU، Zabbix فقط‌خواندنی و رابط سادهٔ پاسخ | ۳ | سؤال تازهٔ فارسی و انگلیسی با اینترنت قطع پاسخ محلی و مستند بگیرد؛ ZBX-01 تا ZBX-08 و موارد قابل‌اعمال OFF-01 تا OFF-10 قبول شوند. |
| دو — توضیح رخداد با Linux و Zabbix | افزودن تاریخچه و رخداد محدود و بررسی مستقیم Linux به مسیر موجود | ۳ | بررسی با شبیه‌ساز و آزمایشگاه مجاز؛ آزمون شروع مجدد و آفلاین؛ تغییر غیرفعال. |
| سه — شبکه و پایش | Windows، Cisco، Juniper، Grafana، موجودی و توپولوژی مستند و جداسازی پیشنهادی پایگاه | ۴، پیشنهاد اصلی | قرارداد نسخه‌دار، آزمون محدود و جداسازی خرابی اتصال؛ آزمون انتقال و بازیابی پایگاه. |
| چهار — دیوار آتش | بررسی VPN، مسیر و سیاست FortiGate و Sophos | ۴ | محدودیت نسخه و API تأییدشده، شواهد چندتجهیزی و نبود تغییر غیرمجاز. |
| پنج — پایگاه داده و مجازی‌سازی | SQL Server، MySQL/MariaDB و ESXi | ۴ | حساب و پرس‌وجوی فقط‌خواندنی و محدودیت مستند نسخه و مجوز. |
| شش — دانش و تحلیل علت | حافظهٔ رخداد، بازیابی محلی، هم‌بستگی توپولوژیک و ارزیابی دوزبانهٔ بهتر | ۴ | ارزیابی مستقل کیفیت، کنترل تازگی و دامنه و بودجهٔ سنجیدهٔ CPU. |
| هفت — اصلاح کنترل‌شده | دستورالعمل مصوب، تأیید دقیق، بررسی نتیجه، رفع ابهام و بازگردانی؛ اجرای تغییر در ماشین جدا | ۵ در صورت فعال شدن | آزمون بازپخش، تغییر وضعیت بین تأیید و اجرا و نتیجهٔ نامشخص؛ تأیید آزمایشگاه پیش از تغییر عملیاتی. |
| هشت — احراز آمادگی بهره‌برداری | استقرار سخت‌سازی‌شده، رابط و راهنمای کامل، بستهٔ آفلاین، انتشار و بازگشت، پشتیبان مستقل و تمرین بازیابی | ۵ با اصلاح؛ ۴ فقط‌خواندنی | چک‌لیست بازبینی‌شده، محدودهٔ کارکرد و RPO/RTO اندازه‌گیری‌شده و پذیرش خطر تک‌میزبان. |

تعدادها مربوط به یک محیط سرویس‌دهی‌اند، نه میزبان فیزیکی یا خانوادهٔ اتصال. Zabbix و تجهیزات موجود، آزمون موقت و پشتیبان مستقل جدا حساب می‌شوند. ماشین چهارم برای جداسازی دسترسی و نگهداری پایگاه پیشنهاد شده، نه نیاز پردازشیِ اثبات‌شده؛ پایلوت کوچک فقط‌خواندنی می‌تواند با استثنای بازبینی‌شدهٔ [برنامهٔ سرورها](SERVER_PLAN.md) روی سه ماشین بماند. افزودن اتصال‌دهنده خودبه‌خود ماشین اضافه نمی‌خواهد.

## گام‌های داخل مرحلهٔ یک

| گام | ماشین و کار اصلی | معیار عبور |
|---|---|---|
| **1A — برنامه و پایهٔ ایمنی** | ماشین برنامه ابتدا ساخته شود؛ قراردادهای دارای نوع، ورود و دامنهٔ محلی، PostgreSQL و مهاجرت، درخواست ماندگار، ممیزی، رابط و API حداقلی و رد پیش‌فرض با دادهٔ آزمایشی آماده شوند. | ورود و نگهداری وضعیت کار کنند؛ آزمون سیاست، ممیزی و رد عملیات پیش از استفاده از اطلاعات ورود واقعی قبول شود. |
| **1B — سرویس CPU محلی** | ماشین AI دوم ساخته شود؛ مدل و محیط اجرای بازبینی‌شده وارد، هویت سرویس و سقف منابع برقرار و اجرای CPU و بارگذاری آفلاین بررسی شوند. | پاسخ تازهٔ فارسی و انگلیسی و اندازه‌گیری زمان و منابع ثبت شود؛ این هنوز پایان پاسخ‌گویی Zabbix نیست. |
| **1C — شواهد Zabbix** | ماشین اتصال سوم ساخته شود؛ درگاه و فرایند جدا، توکن محدود، عملیات نام‌گذاری‌شدهٔ مجاز، شمارش قطعی و منبع شواهد برقرار شوند. | دادهٔ واقعی و محدود API، شمارش و تازگی درست، رد نوشتن و روش نامجاز و ممیزی پالایش‌شده. |
| **1D — پاسخ سرتاسری** | برنامه، اتصال‌دهنده و مدل با همان سه ماشین به هم متصل شوند. | سؤال تازه پاسخ خوانا و منطبق با واقعیت ثبت‌شدهٔ Zabbix، همراه دامنه، زمان و منبع بگیرد. |
| **1E — پذیرش آفلاین** | اینترنتِ بارهای آزمایش و مرورگر تازه قطع و مسیرهای داخلی مجاز حفظ شوند؛ شروع مجدد، خطا و بار محدود آزموده شوند. | نتیجهٔ ZBX-01 تا ZBX-08 و موارد قابل‌اعمال OFF-01 تا OFF-10، از جمله ورود تازه و شروع و راه‌اندازی دوبارهٔ مجاز، ثبت شود. |

مرحلهٔ صفر و Incrementهای 1 و 2 از 1A شاهد ثبت‌شده در [وضعیت پروژه](../PROJECT_STATE.md) دارند. Increment 3 و Stage 1B بعدی است؛ پذیرش UI و استقرار 1A باز می‌ماند و مراحل بعدی تا ثبت شاهد شروع‌نشده و آزموده‌نشده‌اند. ترتیب ساخت به معنای الزام تکمیل همهٔ قابلیت‌های یک ماشین پیش از ساخت بعدی نیست. قرارداد، اسکلت، hello-world مدل، JSON خام، پاسخ کش‌شده یا شبیه‌ساز به‌تنهایی مرحلهٔ یک را تمام نمی‌کنند.

RAG پیشرفته، SSH مستقیم Linux، داشبورد کامل و ده خانوادهٔ اتصال دیگر نباید اولین پاسخ Zabbix را عقب بیندازند. دسترسی API، وضعیت میزبان‌ها و سلامت موتور پایش جدا باشند. مدل شمارش، مشاهدهٔ زنده یا دادهٔ خودپایشی غایب را نسازد.

## تغییرات صریح و کنترل‌های ثابت

امنیت و اجرای CPU محلی جزو پایه‌اند، نه مراحل پایانی. هر یازده اتصال حفظ شده و به‌شکل مسیر کامل و آزموده تحویل می‌شوند. PostgreSQL مرجع اولیه است و پایگاه داخلی جایگزین پیگیری می‌شود. هوش مصنوعی خارجی غیرفعال است. مدیر راه میان‌بُر ندارد. ادعای علت ریشه‌ای شاهد می‌خواهد و یک G10 یک نقطهٔ خرابی مشترک است. [سوابق تصمیم](../adr/README.md) و [ردیابی](../requirements/TRACEABILITY.md) را ببینید.

توضیح مالک دربارهٔ Zabbix، نیاز اولیهٔ ۱۷ را به مرحلهٔ یک می‌آورد و در مرحلهٔ دو گسترش می‌دهد. این بازنگری، 1A تا 1E را اضافه و مجموع قدیمی ۴۴ vCPU در NEXT_TASK را به **۳۶** اصلاح می‌کند؛ منابع ماشین‌ها افزایش نیافته و پرامپت بایگانی‌شده تغییر نکرده است. [رکورد سخت‌افزار](../requirements/HARDWARE_BASELINE.json) و [یادداشت ESXi](ESXI_BASELINE.md) بر فرض قدیمیِ ۹۰ CPU و یک ترابایت و نامشخص بودن build مقدم‌اند.

## معیار پایان و اقدام بعدی

هر تحویل به کد واقعی دارای نوع، آزمون مرزی با نتیجهٔ واقعی، بازبینی امنیت، مستندات فارسی و انگلیسی، ردیابی و commit نیاز دارد. CPU-only، جداسازی اطلاعات ورود، اجرای محدود و ممیزی همواره برقرار باشند. [الزام آفلاین](OFFLINE_RUNTIME.md) برای هر وابستگی فعال لازم است. مستندات، اثبات پیاده‌سازی یا استقرار نیستند.

[راهنمای شروع](START_HERE.md) را بخوانید و [کارهای باقی‌ماندهٔ مرحلهٔ صفر](../NEXT_TASK.md) را تکمیل کنید. آماده‌سازی فقط پس از مجوز لازم انجام شود. پایلوت فقط‌خواندنی عملیاتی و تغییرات آینده هرکدام تأیید جدا می‌خواهند؛ نبود اطلاعات ورود، هدف بار، فایل سازگار یا مسیر بازیابی مانع صریح است، نه مجوز ساختن واقعیت فرضی.

</div>

<div dir="rtl">

# گزارش مدیریتی وضعیت پروژهٔ NextOps

[English](../en/PROJECT_STATUS_BRIEF.md) · [فهرست مستندات](INDEX.md) · [وضعیت پروژه](../PROJECT_STATE.md) · [کار بعدی](../NEXT_TASK.md)

آخرین به‌روزرسانی: ۳۱ شهریور ۱۴۰۵، برابر با ۲۲ سپتامبر ۲۰۲۶

## جمع‌بندی مدیریتی

NextOps به مرحلهٔ «آزمون کنترل‌شده توسط کاربر» رسیده است. نخستین مسیر کامل و فقط‌خواندنی سامانه روی
چهار سرور Ubuntu 24.04 فعال است: کاربر از طریق پنل خصوصی و رمزگذاری‌شده وارد می‌شود، پرسش خود را به
فارسی یا انگلیسی مطرح می‌کند، دادهٔ جاری را از Zabbix و از مسیر اتصال‌دهندهٔ محدود دریافت می‌کند و
در پایان، توضیحی مبتنی بر همان شواهد را از مدل محلی و صرفاً مبتنی بر CPU می‌گیرد.

این نسخه برای آزمون هدایت‌شدهٔ کاربران آماده است، اما هنوز نباید «آمادهٔ بهره‌برداری نهایی» معرفی
شود. دامنهٔ کنونی به پایش یک میزبان تأییدشده و یک عملیات خلاصه‌سازی محدود است. ثبت ماندگار سابقهٔ
بررسی‌ها، پذیرش در شرایط قطع WAN، تمرین پشتیبان‌گیری و بازیابی، آزمون سناریوهای خطا و گسترش دامنهٔ
میزبان‌های پایش‌شده همچنان باقی مانده است.

## نمای کلی وضعیت

| حوزه | وضعیت کنونی | نتیجهٔ تأییدشده | شرط باقیمانده برای پذیرش |
|---|---|---|---|
| معماری و حاکمیت | مبنای مصوب | معماری چهارسروری، پردازش محلی صرفاً روی CPU و اصل «ابتدا فقط‌خواندنی» همچنان اعمال می‌شود | ثبت ممیزی هر عملیات و تکمیل آزمون‌های بازیابی |
| برنامه و پایگاه داده | فعال برای آزمون کنترل‌شده | نسخهٔ تغییرناپذیر برنامه، مهاجرت PostgreSQL 16، هویت محلی، TLS خصوصی، پنل دوزبانه، API و تونل هوش مصنوعی فعال‌اند | افزودن ثبت ماندگار بررسی‌ها، شواهد پشتیبان‌گیری و بازیابی و پایش عملیاتی |
| هوش مصنوعی محلی | فعال و یکپارچه | نسخهٔ تثبیت‌شدهٔ llama.cpp و مدل Qwen روی رابط محلی فعال‌اند و تولید پاسخ فارسی و انگلیسی از مسیر برنامه موفق بوده است | تکمیل آزمون کیفیت، بار، لغو، راه‌اندازی مجدد، بازگشت نسخه و قطع اینترنت |
| Zabbix | فعال با دامنهٔ محدود | Zabbix 7.0.30، PostgreSQL، رابط وب و Agent 2 فعال‌اند؛ حساب API تنها به یک میزبان تأییدشده و سه روش خواندن دسترسی دارد | گسترش کنترل‌شدهٔ میزبان‌ها و تکمیل سیاست نگه‌داری، پشتیبان‌گیری، بازیابی و آزمون خطا |
| اتصال‌دهندهٔ فقط‌خواندنی | فعال با حداقل دسترسی | سرویس بدون دسترسی root، فقط روی رابط محلی، با TLS سخت‌گیرانه، اطلاعات محرمانهٔ محافظت‌شده و عملیات ازپیش‌مجاز کار می‌کند | افزودن ممیزی و پایش اتصال‌دهنده و اجرای سناریوهای توکن باطل، پایان مهلت، دادهٔ قدیمی و بازیابی |
| مسیر سرتاسری کاربر | در شرایط عادی شبکهٔ داخلی موفق | دریافت شواهد زنده و تولید پاسخ مستند در هر دو زبان فارسی و انگلیسی موفق بوده است | تکرار آزمون روی دستگاه تازه با WAN مسدود و ثبت کامل شواهد مرحلهٔ 1E |

## قابلیت تحویل‌شده برای آزمون کاربر

- پنل خصوصی HTTPS با رابط چپ‌به‌راست انگلیسی و رابط راست‌به‌چپ واقعی فارسی.
- ورود محلی محافظت‌شده؛ هیچ‌یک از اطلاعات ورود زیرساخت در اختیار مرورگر یا مدل قرار نمی‌گیرد.
- گردش کار ثابت و فقط‌خواندنی به‌جای دسترسی دلخواه به JSON-RPC، نشانی شبکه، فرمان سیستم یا عملیات
  تغییردهنده.
- نمایش شواهد جاری Zabbix شامل نسخهٔ منبع، میزبان، زمان گردآوری، زمان مشاهدهٔ هر سنجه، وضعیت تازگی
  داده و شمار رخدادهای فعال.
- تولید توضیح توسط مدل محلی روی CPU و بر پایهٔ شواهد بازگردانده‌شده، بدون مسیر جایگزین ابری.
- استقرار بومی با systemd، نسخه‌های تغییرناپذیر، پیوندهای پایدار، پیکربندی و اطلاعات محرمانهٔ متعلق
  به root، سرویس‌های داخلی محدود به loopback، TLS خصوصی و دیوارهٔ آتش میزبان.
- تونل‌های SSH مجزا و محدود از سرور برنامه به سرویس‌های هوش مصنوعی و اتصال‌دهنده.

## شواهد پذیرش تا این مرحله

- بررسی قالب و کیفیت کد با Ruff، نوع‌سنجی سخت‌گیرانه با mypy روی ۵۷ فایل، اعتبارسنجی مستندات و
  استقرار و ۹۳ آزمون خودکار موفق بوده است. پنج آزمون یکپارچهٔ PostgreSQL در محیط رومیزی، به‌دلیل
  تعریف نشدن نشانی پایگاه دادهٔ محلی، اجرا نشد.
- آزمون اتصال‌دهنده نسخهٔ Zabbix 7.0.30، یک میزبان مجاز، هشت سنجهٔ جاری، صفر رخداد فعال و صفر سنجهٔ
  قدیمی را گزارش کرد.
- آزمون احرازهویت‌شدهٔ سرتاسری برای پایش زنده و تولید پاسخ مستند در هر دو زبان موفق بود. درخواست
  فارسی و انگلیسی هر یک پاسخ ۱۲۸ توکنی مطابق محدودیت تنظیم‌شده دریافت کردند.
- بازبینی مرورگری، صفحهٔ ورود انگلیسی و چیدمان راست‌به‌چپ واقعی فارسی را تأیید کرد. گردش کامل
  احرازهویت‌شده نیز به‌صورت برنامه‌ای روی نشانی HTTPS مستقرشده اجرا شد.
- در زمان استقرار، هر چهار سرور از نظر systemd سالم بودند و هیچ واحد ناموفق، بستهٔ به‌روزرسانی‌نشده
  یا نیاز به راه‌اندازی مجدد گزارش نشد.

## وضعیت امنیتی

اختیار نسخهٔ کنونی عمداً محدود نگه داشته شده است. اطلاعات ورود Zabbix فقط روی سرور اتصال‌دهنده
قرار دارد؛ API اتصال‌دهنده فقط از loopback در دسترس است؛ نقش API تنها روش‌های `host.get`،
`item.get` و `problem.get` را برای یک گروه میزبان تأییدشده مجاز می‌داند؛ و رد شدن یک روش خارج از
فهرست مجاز نیز عملاً آزموده شده است. مدل فقط شواهد نرمال‌شده را دریافت می‌کند، به اطلاعات ورود
زیرساخت دسترسی ندارد و قادر به تغییر سامانه یا تأیید عملیات نیست. دیوارهٔ آتش هر میزبان نیز تنها
SSH و نقطهٔ پایانی خصوصی HTTPS موردنیاز را مجاز می‌داند.

## کارهای باقی‌مانده تا پذیرش نهایی

۱. هر بررسی، ارجاع به شواهد، خروجی مدل و تصمیم امنیتی در مخزن ماندگار اجرای کار و سابقهٔ ممیزی
صرفاً‌افزودنی ثبت شود.

۲. میزبان‌ها، الگوها و شواهد سطح خدمتِ موردنیاز با تصویب دامنه افزوده شوند، بدون آنکه اتصال‌دهنده
فراتر از عملیات خواندنِ صریحاً بازبینی‌شده اختیار پیدا کند.

۳. مرحلهٔ 1E با WAN مسدود تکمیل شود: راه‌اندازی سرد، ورود تازه، قطع وابستگی‌ها، توکن باطل، دادهٔ
قدیمی، کمبود فضا، محدودیت منابع و آزمون از یک دستگاه تازه.

۴. رویه‌های مستقل پشتیبان‌گیری و بازیابی برای PostgreSQL، Zabbix، پیکربندی و نسخه‌های نرم‌افزار
ایجاد و عملاً تمرین شوند؛ سپس بازگشت نسخه برای هر نقش اثبات شود.

۵. داشبوردهای عملیاتی، هشدارها، سیاست نگه‌داری گزارش‌ها، چرخش گواهی و کلید و فرایند ثبت بازخورد و
نقص‌های آزمون کاربر تکمیل شوند.

## نتیجهٔ قابل ارائه

NextOps اکنون به‌جای زیرساخت صرفاً آماده‌شده، یک برش عملیاتی و دارای مرز امنیتی روشن در اختیار دارد.
سامانه برای آزمون هدایت‌شدهٔ تجربهٔ دوزبانه و فقط‌خواندنی آماده است. اعلام آمادگی برای بهره‌برداری
نهایی باید تا تکمیل ثبت ماندگار ممیزی، گسترش کنترل‌شدهٔ دامنهٔ پایش، پذیرش در شرایط قطع اینترنت و
خطا و اثبات پشتیبان‌گیری، بازیابی و بازگشت نسخه به تعویق بماند.

</div>

# Copy to /etc/nextops/certificate-check.env as root-owned mode 0644.
# The certificate is public material; never place the private-key path here.
NEXTOPS_CERTIFICATE_PATH=/absolute/path/to/frontend-certificate.crt
NEXTOPS_CERTIFICATE_LABEL=deployment-owned-label
NEXTOPS_CERTIFICATE_WARNING_DAYS=90
